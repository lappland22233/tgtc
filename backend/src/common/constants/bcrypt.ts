/**
 * bcrypt 哈希轮数。
 *
 * 统一所有密码哈希调用使用此常量，避免不同模块使用不同轮数导致安全基线不一致。
 * 轮数越高越安全，但耗时越长（轮数+1，耗时约翻倍）。
 *
 * 参考：
 * - OWASP 推荐 bcrypt 轮数 ≥ 10
 * - 12 轮在现代 CPU 上约 250ms/hash，可抵御暴力破解
 */
export const BCRYPT_ROUNDS = 12;
