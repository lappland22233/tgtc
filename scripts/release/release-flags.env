# 发行清单标志的唯一事实来源（single source of truth）。
#
# 背景：generate-manifest.sh 自 P1/R 批次起 fail-closed——RELEASE_INCLUDES_DB_MIGRATION 与
# RELEASE_ROLLBACK_SAFE 必须显式提供，缺失即 die EXIT_PRECHECK(3)。此前这两个变量需要在每个
# 打包入口各写一遍（release.yml / quality-gates.yml / 未来任何入口），漏改一处就整条发布链路变红。
# 因此统一收敛到本文件：两条流水线在调用 build-linux-x64-release.sh 之前统一 source 本文件。
#
# 使用方式（bash）：
#   set -a; . scripts/release/release-flags.env; set +a
#
# 每次发布前必须按该版本的实际内容复核以下两个标志：
#   RELEASE_INCLUDES_DB_MIGRATION=true|false  本版本是否包含数据库迁移。
#   RELEASE_ROLLBACK_SAFE=true|false          仅切回旧代码（不回退数据库）是否安全。
#     true  仅当本版本不含不可逆迁移；脚本可自动回退程序。
#     false 一旦存在不可逆迁移（如回填），切回旧代码会让数据库停留在新 schema 上；
#           rollback.sh 会拒绝自动回退，需人工恢复数据库备份后显式放行。
# 该标志失实会直接误导更新器的迁移/回退风险判断（历史事故：v1.2.6 含不可逆回填迁移却声明无迁移）。

# 当前版本（1.5.0）：新增「Bot 账号池（多账号回源）」所需副本表与直链源账号锚点的纯增量迁移——
#   180290（PG）：建 telegram_file_copies（uuid 主键显式 DEFAULT gen_random_uuid()、唯一约束
#                 (ownerType, ownerId, accountId)、入站锚点索引 (chatId, messageId)），
#                 并为 telegram_bot_file_grants 补可空列 sourceAccountId；
#   180290（SQLite）：同名 `-Sqlite` 迁移等价补齐（CREATE TABLE IF NOT EXISTS + getTable 判定加列，幂等）；
#   两者均为 expand 式增量（只新增表与可空列，不删改任何旧字段与旧路径），旧代码（1.4.0）
#   可在新 schema 上正常读写。
#   RELEASE_ROLLBACK_SAFE 仍保持 false，理由：（a）本版本尚未执行真实升级/回滚演练，
#   而该标志的失实会直接误导更新器的回退判断；（b）迁移链仍携带 1.2.7 的不可逆回填迁移
#   （180200 撤销私有旧分享链接）；（c）不得因「迁移带 down()」就把本标志置 true。
#   如需放宽，必须人工复核并完成 PG/SQLite 升级→回滚演练后单独提交。
# 上一版本 1.4.0 已含：
#   180260（PG/SQLite）：access_logs 补可空列 transferCompleted / transferAborted / ranged /
#                        terminationReason / responseBodyBytes，用于区分「请求数 / 分段数（206）/
#                        完整完成数 / 中断数」，使续传与中断可在管理后台直接核验。
# 历史版本 1.3.3 已含：
#   180250（PG/SQLite）：建 download_tasks（下载任务持久化，用于重启恢复与过期清理），
#                        PG 侧 uuid 主键显式 DEFAULT gen_random_uuid()。
# 历史版本 1.3.1 已含：
#   180230（PG/SQLite）：建 telegram_bot_file_grants / telegram_bot_daily_usage / telegram_bot_whitelist，
#                       并给 access_logs 补可空列 botGrantId / botTelegramUserId；
#   180240（PG）：补上述三表 uuid 主键 DEFAULT gen_random_uuid()。
# 但仍沿袭 1.2.7 携带的不可逆回填迁移（180200 撤销私有旧分享链接）→ 程序回退不安全。
# 注：若改按「仅本版本迁移」口径评估，本版本或可放宽回退安全标志；此项属发布策略，需人工复核后再改。
# 当前版本（1.5.3）：基于 1.5.2 已有账号池/镜像 schema，收口「账号池统一选号（负载均衡）+ 环境变量主 Bot 可见性」；本版本**无新增迁移**，仅为代码/前端/文档与测试变更——
#   继续兼容 180300/180310/180320（PG/SQLite）已落地的 expand 式结构，
#   不删改旧字段与旧路径，旧代码可在新 schema 上正常读写。
#   RELEASE_ROLLBACK_SAFE 维持 false，理由：（a）本版本尚未执行真实升级/回滚演练，
#   该标志的失实会直接误导更新器的回退判断；（b）迁移链仍携带 1.2.7 的不可逆回填
#   （180200 撤销私有旧分享链接）；（c）不得因「迁移带 down()」就把本标志置 true。
RELEASE_INCLUDES_DB_MIGRATION=true
RELEASE_ROLLBACK_SAFE=false
